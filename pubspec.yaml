name: wishey_restart
description: make you wish wishey

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  firebase_core: ^0.5.3
  firebase_auth: ^0.18.4+1
  firebase_analytics: ^6.3.0
  flutter:
    sdk: flutter
  get: ^3.23.1
  get_storage: ^1.4.0
  regexpattern: ^1.0.0

dev_dependencies:
  pedantic: ^1.9.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/present_asset.jpg